hydra:
  run:
    dir: eval_out/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: eval_out/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.override_dirname}
  job:
    chdir: True

model_path: null
N_clean: 1
N_noisy: 3
seed: 1
save_output: false
num_inference_steps: 1
eval:
  split: test
